# Admin Template UI

React + TypeScript + Vite + PrimeReact prototype for an empty administrative workflow UI.

## Run locally

```bash
npm install
npm start
```

Then open:

```text
http://127.0.0.1:5173/
```

You can also run:

```bash
npm run dev
npm run build
```
